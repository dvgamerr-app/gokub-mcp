# Binaries for programs and plugins
/gokub
/gokub-mcp
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
# vendor/
.env
docs/ASSIGNMENT.md

# trade journal (runtime data)
trades.json

# Go vendor dir (generate on demand with `go mod vendor` for offline/hermetic Docker builds)
vendor/

agents/